# EXECPLAN Anchor (Read First)

Before doing ANY work:
1) Read PLANS.md at repository root.
2) Read and follow all instructions under .agents/ (EXECPLAN rules).
3) Then read and follow this prompt pack in numeric order.

Operating rules:
- Implement milestone-by-milestone.
- At the end of EACH milestone:
  - The system must be runnable locally AND via Docker (API + UI).
  - dotnet test must pass.
  - Produce a "Milestone Commit Notes" message suitable for manual commit.
  - Update prompts/01-InitialDevelopment/state/* accordingly.

