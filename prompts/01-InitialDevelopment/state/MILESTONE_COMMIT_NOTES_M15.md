M15: add unified decisions system, toolbox UI, and single-select toolbox menu

- add Decisions entity/DTOs/migration plus decision service + controller endpoints
- add decision toolbox components with pending/resolved lists, notes dialog, and selection controls
- switch toolbox visibility to a single-select menu (Decisions/clear) and widen toolbox panel
- tighten decision discovery to source-defined interfaces to avoid test hangs
- update UI architecture docs and adjust repository tree font sizing
