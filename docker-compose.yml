services:
  api:
    build:
      context: .
      dockerfile: src/SilkHat.Api/Dockerfile
    ports:
      - "5000:8080"
    environment:
      - ConnectionStrings__SilkHat=Data Source=/data/silkhat.db
    volumes:
      - silkhat-data:/data
  ui:
    build:
      context: .
      dockerfile: src/SilkHat.Ui/Dockerfile
    depends_on:
      - api
    ports:
      - "5001:80"
volumes:
  silkhat-data:
